import { LocationsFilterPipe } from './locations-filter.pipe';

describe('LocationsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
